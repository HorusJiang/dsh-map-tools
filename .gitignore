# ===== 敏感配置（绝不提交——含 API key） =====
# 运行时的 API 配置存放在用户目录 ~/.dsh-map-tools/config.json（不在仓库内）。
# 以下路径是双保险：即使有人把配置复制进工作区，也不会被 git 跟踪。
.dsh-map-tools/
config.json
config*.local.json
*.config.json

# 环境变量 / 密钥
.env
.env.*
!.env.example
*.pem
*.key
*.p12
secrets/

# Dependencies
node_modules/

# Local pnpm store (project-local v11 store created by pnpm install; keep out of git)
.pnpm-store/

# Build output
lib/

# Test / coverage output
coverage/
.vitest/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local reference files
.contributing.md

# Local session handoff (not for public repo)
PROJECT-SNAPSHOT.md

# 内部材料：只留在本地，不进公开仓库（曾误提交，已从索引移除）
PLAN.md
RELEASE-STATUS.md

# docs/ 是本地材料目录：默认整个目录不进版本控制（曾把内部的思路性文档误提交过一次，
# 已从历史剔除）。已跟踪的三个例外是历史遗留，保留不动；以后新增的 docs 文件一律忽略。
docs/**
!docs/awesome-dsh-plugin-submission.yml
!docs/dsh-map-tools-v0.1.3兼容性说明.md
!docs/开发-热插拔-HMR.md
